收到。相册管理入口保留在“相册”标题行右侧，同时优化“筛选”区的布局与整体对齐。

## 具体改动
- 相册区标题行：
  - 将“新建/管理相册…”入口移动到“相册”Section 的 header 右侧（HStack：左标题、右按钮），点击后显示现有 AlbumManagementView。
- 相册区内容：
  - 保留相册列表，不再在列表底部放置该入口按钮。
- 筛选区布局：
  - 调整为两行整洁分组：第一行显示旗标 + 评分；第二行显示颜色标签；统一元素间距与行高，左对齐。
- 整体对齐：
  - 使用统一的 `.listRowInsets` 让“图库/相册/筛选”的非树形行，与“文件夹”树的左基线保持一致；如需微调，会把该值作为常量（例如 22pt），并在本次实现后根据视觉再校准。

## 修改文件
- SidebarView.swift：
  - 调整“相册”Section 的 header，添加右侧管理入口按钮；
  - 改造 SidebarFiltersView 的元素排版为两行分组；
  - 对相关 Section 应用统一 `.listRowInsets`。

确认后我开始实施以上改动，并在完成后自检对齐与整洁性。