## 目标
- 左右侧边栏在应用启动和视图切换时，默认占用其最小宽度（minWidth）。

## 现状
- 左侧主侧栏：已为最小（min=180，ideal=180，max=300），见 [ContentView.swift:L57](file:///Users/bytedance/workspace/ai-demo/culler/Culler/Culler/Views/ContentView.swift#L57)
- 右侧信息面板：虽 min=220、ideal=220，但设置了 .layoutPriority(2)，导致默认更易扩张，见 [ContentView.swift:L139-L143](file:///Users/bytedance/workspace/ai-demo/culler/Culler/Culler/Views/ContentView.swift#L139-L143)
- 专辑管理左栏：已为最小（min=200，ideal=200，max=300），见 [AlbumManagementView.swift:L61-L63](file:///Users/bytedance/workspace/ai-demo/culler/Culler/Culler/Views/AlbumManagementView.swift#L61-L63)

## 调整方案
- 将右侧信息面板的 .layoutPriority 从 2 降为 0（或 1），让中间内容区优先占据剩余空间，从而右栏保持最小宽度：修改 [ContentView.swift:L141-L143](file:///Users/bytedance/workspace/ai-demo/culler/Culler/Culler/Views/ContentView.swift#L141-L143)
- 保持左侧栏现有设置不变（已默认最小）。
- 如需进一步缩小最小值，可同步调整 minWidth/idealWidth 常量值（例如将 220 改为更小数字）。

## 验证
- 运行后在 Grid/Single/Fullscreen 模式切换，观察左右栏初始占位是否保持最小值。
- 拖动分隔线确认最小/最大边界仍有效。